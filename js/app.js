const swiper = new Swiper('.swiper', {
    direction: 'vertical',
    speed: 1500,
    parallax: true,
    mousewheel: {
        invert: true,
      },
      keyboard: {
        enabled: true,
        onlyInViewport: false,
      }
});